export default function Chart({ title }) {
  return (
    <div style={{ border: "1px solid #ddd", padding: "1rem", borderRadius: "8px" }}>
      <h4>{title}</h4>
      <p>Chart goes here</p>
    </div>
  );
}
